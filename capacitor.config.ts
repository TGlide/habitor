import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.thomasglopes.habitor',
	appName: 'habitor',
	webDir: 'build',
	bundledWebRuntime: false,
};

export default config;
